import MainLayout from "@/layout/MainLayout";
import CartSection from "@/sections/CartSection";

const Cart = () => {
  return (
    <MainLayout>
      <CartSection />
    </MainLayout>
  );
};

export default Cart;
